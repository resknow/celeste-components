<img
    class="ResponsiveImage {{ class }}"
    src="{{ default }}"
    width="{{ width }}"
    height="{{ height }}"
    loading="{{ loading | default('lazy') }}"
    srcset="{{ srcset }}"
    alt="{{ alt }}"
>